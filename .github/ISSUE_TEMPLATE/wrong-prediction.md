---
name: A prediction was wrong
about: zc said one speed and the model ran at another
title: 'wrong prediction: '
labels: prediction
---

<!--
A wrong prediction is data, not a complaint. This is the most useful issue you
can open.

Both commands below run offline and carry no hostname, username, serial, MAC,
IP or file path.
-->

**What zc predicted, and what actually happened**

```
paste `zc verify` output here -- it prints both
```

**Machine details**

```
paste `zc doctor` output here (it is Markdown, so it renders)
```

**Anything unusual about the run?**

e.g. on battery, thermally throttled, other heavy processes, a non-default
context length or thread count, models on an external or network drive.
